- name: Install zsh
  become: true
  apt:
    name: zsh
    state: present
  tags: &tags_for_zsh_task
    - install
    - core
    - zsh

- name: Change shell to zsh
  shell: chsh -s $(which zsh)
  tags: *tags_for_zsh_task